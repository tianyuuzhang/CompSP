# 直观感受

Qualitative examples and visual artifacts used for manual inspection.

Large text files may contain sampled unsafe/long-answer examples; treat them as analysis provenance rather than reusable pipeline inputs.
