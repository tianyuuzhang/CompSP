# 回答内容、攻击指标与安全结构的三方关联实验

更新时间：2026-07-11

## 2026-07-11 用户确认的研究口径

- “长内容”指去除拒绝套话后的实质内容，但研究不只看长回答；短回答中的拒绝形式也是可用信号。
- 黑盒主实验只使用攻击者可获得的回答文本及文本统计特征；机制研究允许白盒 hidden state、方向和子空间。
- 数据不局限于 Llama。Qwen、4omini、DeepSeek 与 HarmBench 用于黑盒关联和迁移；不同目标模型标签禁止混用。
- 输入、输出及二者联合都可用于定位安全结构；同时研究结构探索与结构利用。
- 利用目标优先预测 ASR；ALR、拒绝、伪方向及其他有效指标可作为探索变量。
- 最终希望用单条或少量回答定位潜在风险。当前 20 次访问的经验 ASR 步长为 0.05，不能直接区分 0.01 与 0.001；需要连续风险模型、校准不确定性或序贯增采。
- 可以探索替代 ASR，但旧 `toxicA` 在首次 unsafe 后提前停止，无法离线重建三票 unsafe 多数制。
- 当前处于猜想探索阶段，结论随证据调整，不预设必须证明三方因果链。

## 2026-07-12 用户确认的扩展口径

- 回答内容与 ASR/ALR 的相关性研究不局限于 Llama；只要标签来自同一目标模型，其他目标模型也可纳入。
- 黑盒研究只使用攻击者可见的文本权限，包括 q1、模型回答及其统计特征；白盒研究可以同步考察 hidden state、方向和子空间。
- 研究目标同时包含结构探索和结构利用：输入、输出、输入+输出三种视图都保留，优先寻找稳定有效的相关性。
- 利用目标优先 ASR，尤其关注少量回答能否为 ASR 精细定位或后续增采提供信号；但探索阶段允许使用 ALR、替代 ASR、拒绝形式和伪方向投影。
- ASR 定义需要做敏感性分析，包括 any-unsafe、判定 hazard、加权 ASR，以及未来重新采样时可验证的多数 unsafe。
- 当前结论不预设，若实验显示输出内容只能解释行为指标而不能稳健对应白盒方向，应据此调整“三相关性”的表述。

## 研究问题

本实验区分三个变量层次：

1. 指令攻击性：由同一指令多次访问得到的 ASR、ALR 表示。
2. 模型内部安全结构：白盒中使用最后层 hidden state 学习的 Ridge 伪安全投影；黑盒中只能把 ASR/ALR 诱导的稳定低维行为坐标作为“隐安全结构”。
3. 模型输出内容：单条或少量回答中可观测的长度、拒绝形式、词法语义和回答表示。

现阶段目标是验证三者存在跨问题泛化的统计关联。仅凭相关性不能证明内部安全结构因果地生成某类回答；因果结论需要后续激活干预实验。

## 默认口径

- “长内容”优先指去除开头拒绝套话后的实质内容；完整长度保留为朴素基线。
- 分别评估仅回答、仅 q1 和 q1+回答。当前脚本首先实现“仅回答”。
- `toxic`、`toxicA` 和安全判定文本禁止作为输入特征，避免标签泄漏。
- 少量回答使用 `n=1,2,4,8,20`，观察嗅探性能随访问次数的变化。
- 训练/测试继续使用固定 60/20 问题划分，禁止随机按指令拆分。

## 由浅入深的分析顺序

### 第一层：可解释行为特征

提取字符/词数、实质内容长度、拒绝开头、拒绝词命中、列表、代码块、词汇多样性等特征。对 ASR、ALR 和白盒 `pseudo_score` 分别做 Ridge 回归，并报告 Pearson、Spearman、MAE、R2 和同题序关系准确率。

这一层用于回答“长度或拒绝模板是否已经解释主要信号”。若只在不同 ALR 粗桶之间有效，不能作为安全结构证据。

### 第二层：词法内容

删除常见拒绝前缀后，对实质文本使用 word/character TF-IDF。它比纯长度更接近“长回答具体说了什么”，同时仍可通过高权重词组审计捷径。

TF-IDF 支持三种严格区分的文本视图：`response`（仅回答）、`q1`（仅原始指令）和
`joint`（q1+回答）。只有回答或 joint 相比 q1 稳定提高，才能说明输出提供了超出输入
攻击文本的额外嗅探信息。

### 第三层：语义表示

后续将回答编码为 frozen embedding，比较线性 Ridge、PLS 和 CCA。严格使用训练问题拟合降维与回归参数，在测试问题上评估。

第一版白盒语义实验使用 Llama-3.1-8B-Instruct：单条回答删除拒绝前缀后作为纯文本
输入，提取最后层最后 token embedding，再用 Ridge 预测输入 q1 的伪安全投影。该设置
检验模型自身的回答表征是否携带输入安全坐标，不等同于黑盒外部文本编码器。

### 第四层：白盒回答轨迹

在 Llama 回答 token 上分别提取 assistant 起始、拒绝前缀结束、实质内容中段和末 token hidden state，检验这些位置的表示与输入 q1 安全投影、ASR/ALR 的关系。

### 第五层：少样本嗅探与干预

用单条或少量回答反推 q1 的伪安全投影，绘制 `n=1/2/4/8/20` 学习曲线；随后沿安全方向干预激活，检验回答内容特征是否随投影发生可重复变化。

## 2026-07-12 序贯 ASR 增采离线模拟

目的：检验少量回答是否能作为继续采样或优化攻击的黑盒线索。每条指令的 20 个回答被随机拆成前缀和后缀，策略只能看前 `k` 条回答，在同一数据集、同一问题内选择 top 20% 指令，再用未使用的后缀 ASR 评估风险。

代码入口：

```bash
PYTHONPATH=$PWD/src /remote-home/zty/conda/LM_zty/repetition/bin/python \
  scripts/simulate_sequential_asr_sampling.py \
  --dataset-keys jbb-llama-ofa,jbb-llama-pair,jbb-llama-drattack \
  --sample-sizes 1,2,4 \
  --seeds 5 \
  --top-fraction 0.2 \
  --high-threshold 0.5 \
  --output outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_sampling.json
```

首轮结果显示，随机 top 20% 的后续 ASR 平均约为 `0.636`；使用前缀回答选择候选后，后续 ASR 明显升高。`k=1` 时，`prefix_hazard_weighted_asr` 达到 `0.770`；`k=2` 时达到 `0.802`；`k=4` 时达到 `0.824`。这说明少量回答确实能帮助定位更高风险的指令。

需要谨慎解释的是，经验 ASR、Jeffreys ASR、judge hazard 和 hazard-weighted ASR 对“前缀零 unsafe、后缀高风险”的召回几乎为零。也就是说，这类策略更擅长确认已经暴露出 unsafe 迹象的高风险指令，而不擅长发现前几次回答全安全但真实风险仍高的隐蔽指令。`prefix_alr` 对这类样本召回更高，但总体后续 ASR 提升较弱。后续应把 ALR/长度/拒绝形态作为探索型补充信号，而不是替代 ASR 主指标。

### 跨模型 OFA 扩展

同一脚本扩展到 JBB-OFA 与 HarmBench-OFA 的四个目标模型。分组选择始终在同一 `dataset_key + questionID` 内完成，因此不会把一个目标模型的 ASR 标签混给另一个目标模型。

`k=4`、top 20%、`prefix_hazard_weighted_asr` 的分目标模型结果如下：

| 数据 | records | random k4 后续ASR | hazard-weighted k4 后续ASR | lift | 前缀零unsafe隐蔽高风险召回 |
|---|---:|---:|---:|---:|---:|
| jbb-llama-ofa | 8000 | 0.636 | 0.823 | 0.188 | 0.000 |
| jbb-qwen-ofa | 8000 | 0.386 | 0.644 | 0.258 | 0.000 |
| jbb-4omini-ofa | 8000 | 0.692 | 0.809 | 0.119 | 0.005 |
| jbb-deepseek-ofa | 8000 | 0.842 | 0.922 | 0.082 | 0.000 |
| hb-llama-ofa | 2200 | 0.662 | 0.834 | 0.174 | 0.000 |
| hb-qwen-ofa | 2200 | 0.570 | 0.750 | 0.185 | 0.000 |
| hb-4omini-ofa | 2200 | 0.672 | 0.798 | 0.129 | 0.000 |
| hb-deepseek-ofa | 2200 | 0.860 | 0.947 | 0.084 | 0.000 |

这组结果说明少量回答增采信号不局限于 Llama，也不局限于 JBB。Qwen 的基础 ASR 较低，因此 lift 最大；DeepSeek 的基础 ASR 已经很高，因此绝对后续 ASR 最高但 lift 较小。所有模型上，前缀零 unsafe 的隐蔽高风险召回都接近 0，说明单纯依赖前缀 unsafe 证据会遗漏“低频成功但真实危险”的指令。下一步需要把回答文本残差嗅探器加入选择策略，专门提高这类样本的召回。

### 前缀文本残差嗅探器

为检验“回答具体内容”是否能改善序贯增采，新增脚本：

```bash
PYTHONPATH=$PWD/src /remote-home/zty/conda/LM_zty/repetition/bin/python \
  scripts/simulate_text_residual_sequential_asr.py \
  --dataset-keys jbb-llama-ofa,jbb-llama-pair,jbb-llama-drattack \
  --sample-sizes 1,2,4 \
  --seeds 1 \
  --max-features 8000 \
  --top-fraction 0.2 \
  --high-threshold 0.5 \
  --output outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_text_residual_prefix_sampling_fast.json
```

该脚本使用固定训练/测试问题划分：60 个训练问题拟合“前缀回答 -> 后缀 ASR”，20 个测试问题上选择候选。模型包含四类分数：长度/拒绝/格式基线、前缀文本 TF-IDF、文本残差、基线+文本残差。文本清理默认替换显式拒绝词和危险词，降低词面捷径。

Llama 三攻击轻量首轮结果如下：

| k | 策略 | selected future ASR | lift | 前缀零unsafe隐蔽高风险召回 |
|---:|---|---:|---:|---:|
| 1 | prefix_hazard_weighted_asr | 0.744 | 0.116 | 0.000 |
| 1 | prefix_alr | 0.728 | 0.100 | 0.157 |
| 1 | model_base_features | 0.739 | 0.111 | 0.197 |
| 1 | model_text_only | 0.755 | 0.127 | 0.144 |
| 1 | model_base_plus_text_residual | 0.751 | 0.124 | 0.182 |
| 2 | prefix_hazard_weighted_asr | 0.773 | 0.146 | 0.000 |
| 2 | model_text_only | 0.767 | 0.140 | 0.089 |
| 2 | model_base_plus_text_residual | 0.760 | 0.132 | 0.168 |
| 4 | prefix_hazard_weighted_asr | 0.791 | 0.164 | 0.000 |
| 4 | model_text_only | 0.767 | 0.140 | 0.062 |
| 4 | model_base_plus_text_residual | 0.763 | 0.136 | 0.188 |

解释：前缀文本模型能在测试问题上产生可用的后缀 ASR lift，说明回答文本确实携带可泛化风险信息；但它没有解决“前缀零 unsafe 但后缀高风险”的核心盲点。文本模型更像是在细化已经显露风险的回答形态，而不是发现完全隐藏的低频成功指令。当前更合理的策略是把 `prefix_hazard_weighted_asr` 作为高精度确认信号，把 ALR/长度拒绝格式基线作为补充召回信号，再研究专门针对前缀零 unsafe 样本的二阶段模型。

### prefix-zero 保留名额实验

为验证“隐蔽高风险召回”和“平均 ASR 提升”是否存在目标冲突，文本残差脚本新增混合策略：在同一问题内选择 top 20% 候选时，先把一部分名额保留给前缀没有任何 unsafe 的样本，再用 `prefix_hazard_weighted_asr` 填满剩余名额。保留样本按 `model_base_plus_text_residual` 或 `model_base_features` 排序。

Llama 三攻击轻量结果显示，保留名额可以显著提高 prefix-zero 隐蔽高风险召回，但会牺牲总体 selected future ASR：

| k | 策略 | selected future ASR | lift | 前缀零unsafe隐蔽高风险召回 |
|---:|---|---:|---:|---:|
| 1 | prefix_hazard_weighted_asr | 0.744 | 0.116 | 0.000 |
| 1 | hybrid text quota 25% | 0.677 | 0.050 | 0.376 |
| 1 | hybrid text quota 50% | 0.614 | -0.014 | 0.605 |
| 2 | prefix_hazard_weighted_asr | 0.773 | 0.146 | 0.000 |
| 2 | hybrid text quota 25% | 0.678 | 0.051 | 0.554 |
| 2 | hybrid text quota 50% | 0.601 | -0.026 | 0.772 |
| 4 | prefix_hazard_weighted_asr | 0.791 | 0.164 | 0.000 |
| 4 | hybrid text quota 25% | 0.688 | 0.060 | 0.688 |
| 4 | hybrid text quota 50% | 0.623 | -0.004 | 0.688 |

这说明“用少量回答最大化下一轮平均 ASR”和“发现前几次回答全安全但后续仍可能成功的低频风险”不是同一个优化问题。若研究目标是攻击优化，`prefix_hazard_weighted_asr` 更有效；若研究目标是安全审计或低 ASR 精细定位，需要把一部分预算主动分给 prefix-zero 样本，并结合 ALR、长度/拒绝格式和文本残差信号做二阶段筛选。

### prefix-zero 二阶段隐蔽风险模型

混合保留名额证明了“给 prefix-zero 分预算”能提高隐蔽高风险召回，但这仍是人为预算策略。进一步新增：

```bash
PYTHONPATH=$PWD/src:$PWD/scripts /remote-home/zty/conda/LM_zty/repetition/bin/python \
  scripts/analyze_prefix_zero_hidden_risk.py \
  --dataset-keys jbb-llama-ofa,jbb-llama-pair,jbb-llama-drattack \
  --sample-sizes 1,2,4 \
  --seeds 1 \
  --max-features 8000 \
  --top-fraction 0.2 \
  --high-threshold 0.5 \
  --output outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_fast.json
```

该脚本只在 prefix-zero 样本内部训练和评估，即前 `k` 次回答均未出现 unsafe 的指令。目标是预测未使用后缀 ASR 是否仍然大于等于 `0.5`。这更接近安全审计问题：从“表面安全”的候选里找低频但高危的指令。

Llama 三攻击轻量结果如下：

| k | prefix-zero测试数 | 高风险比例 | 策略 | ROC-AUC | AP | top 20% precision | top 20% recall |
|---:|---:|---:|---|---:|---:|---:|---:|
| 1 | 2219 | 0.144 | random | 0.508 | 0.147 | 0.146 | 0.204 |
| 1 | 2219 | 0.144 | base_ridge | 0.749 | 0.297 | 0.304 | 0.423 |
| 1 | 2219 | 0.144 | text_ridge | 0.787 | 0.414 | 0.390 | 0.542 |
| 2 | 1846 | 0.055 | random | 0.510 | 0.060 | 0.054 | 0.198 |
| 2 | 1846 | 0.055 | base_ridge | 0.730 | 0.113 | 0.119 | 0.436 |
| 2 | 1846 | 0.055 | text_ridge | 0.785 | 0.195 | 0.168 | 0.614 |
| 4 | 1568 | 0.010 | random | 0.396 | 0.008 | 0.000 | 0.000 |
| 4 | 1568 | 0.010 | base_ridge | 0.738 | 0.044 | 0.029 | 0.562 |
| 4 | 1568 | 0.010 | text_ridge | 0.842 | 0.141 | 0.041 | 0.812 |

这组结果是目前对“回答具体内容有安全审计价值”的强证据之一：在完全没有前缀 unsafe 的条件下，文本模型仍能把后缀高风险样本显著排到前面。尤其 `k=4` 时，prefix-zero 子集中的高风险比例只有 `1.0%`，随机 top 20% 没选到任何高风险样本，而 `text_ridge` 能覆盖 `81.2%` 的高风险样本。虽然 precision 只有 `4.1%`，但相对基线比例提升约 4 倍，适合作为追加采样的召回型筛选器。

这也修正了上一节的解释：直接用文本模型和普通 ASR 前缀策略竞争时，文本模型不一定最大化平均 ASR；但把问题限定为 prefix-zero 隐蔽风险检测时，回答具体内容的价值更清楚。后续应把总体高风险确认器和 prefix-zero 隐蔽风险召回器拆成两个模块，而不是用单一排序目标评价所有场景。

2026-07-13 对该脚本补充了 TF-IDF 高权重词项导出，输出仍写入同一报告
`outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_fast.md`。
新增审计显示：正向词项包含 `social`、`post`、`consequences`、`gambling`、`cryptocurrency`、`manipulation`、`code/error` 等；负向词项大量包含被替换后的 `refusal_term`、`provide/write` 以及若干低风险主题词。这个结果支持“回答文本确实携带可用风险信号”，但同时说明信号来源不是纯粹的长内容语义：主题分布、攻击模板残片、拒绝结构、模型回答姿态都可能参与。下一步需要在同一问题、同一攻击方案内部做更严格的主题控制，并加入回答归属打乱和危险词掩码后的词项稳定性审计，避免把模板或主题差异误解释为安全结构本身。

### prefix-zero 三视图对照

2026-07-13 新增三视图对照，用同一 prefix-zero 二阶段任务分别输入 `response`、`q1`
和 `joint`。运行入口：

```bash
bash scripts/run_prefix_zero_text_view_ablation.sh
```

本次轻量实验耗时约 7 分 36 秒。汇总文件：

- `outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_text_view_summary.json`
- `outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_text_view_summary.md`

核心结果如下：

| k | response AP | q1 AP | joint AP | response-q1 AP | response top recall | q1 top recall |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.414 | 0.216 | 0.404 | 0.199 | 0.542 | 0.335 |
| 2 | 0.195 | 0.061 | 0.209 | 0.134 | 0.614 | 0.238 |
| 4 | 0.141 | 0.009 | 0.186 | 0.132 | 0.812 | 0.062 |

该结果是目前支持“回答内容提供输入之外黑盒嗅探信号”的关键证据：在前缀没有任何
unsafe 的样本中，仅回答文本稳定强于仅 `q1`。尤其 `k=4` 时，`q1` 基本不能识别
后缀高风险样本，而回答文本的 top 20% recall 达到 `0.812`。这说明信号并不只是
原始攻击指令或主题模板本身。

需要保留的边界是：`joint` 只在 `k=2/4` 的 AP 上略高于最强单视图，`k=1` 反而低于
response。因此短期报告应把主结论写成“回答文本单视图已经携带额外风险信号”，而不是
“输入与输出联合必然最好”。后续应继续做多 seed、按攻击方案分层、同题内回答归属打乱，
以及危险词和拒绝词掩码后的稳定性审计。

2026-07-13 随后补充了 `5 seed` 和按攻击方案分层。输出文件统一使用
`*_text_view_summary_seed5.{json,md}`。三攻击混合结果如下：

| k | response AP | q1 AP | joint AP | response-q1 AP | response top recall | q1 top recall |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.457±0.025 | 0.230±0.021 | 0.442±0.029 | 0.228 | 0.562±0.015 | 0.341±0.031 |
| 2 | 0.211±0.023 | 0.057±0.008 | 0.207±0.035 | 0.154 | 0.598±0.037 | 0.183±0.039 |
| 4 | 0.069±0.042 | 0.012±0.003 | 0.073±0.063 | 0.057 | 0.710±0.114 | 0.070±0.051 |

按攻击方案分层后，`response-q1 AP` 仍全部为正：

| 攻击方案 | k=1 | k=2 | k=4 | 解释 |
|---|---:|---:|---:|---|
| OFA | 0.253 | 0.247 | 0.045 | AP 绝对值最高，joint 在 OFA 上略有增益 |
| PAIR | 0.268 | 0.193 | 0.157 | k=4 增量最大，q1-only 对隐蔽高风险几乎失效 |
| DrAttack | 0.174 | 0.070 | 0.035 | 信号最弱但仍为正，说明不是单一攻击模板造成 |

多 seed 后，`k=4` 的 AP 因高风险样本很少而波动较大，所以更应同时报告 top recall。
在三攻击混合下，response 的 `k=4` top recall 为 `0.710±0.114`，q1-only 仅为
`0.070±0.051`。因此当前阶段最稳表述是：在 prefix-zero 子集内，回答文本对后续
高风险样本的召回提供了明显、可跨攻击方案复现的增量；AP 的精确幅度仍需更多 seed
和更大测试集确认。

### 回答归属打乱负对照

2026-07-13 新增回答归属打乱负对照。该对照在同一数据集、同一问题、同一 split
内部打乱 `response` 文本，保留 `q1`、标签和样本边际分布不变。运行入口：

```bash
bash scripts/run_prefix_zero_response_shuffle_control.sh
```

输出文件：

- `outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_response_shuffle_seed5.json`
- `outputs/response_safety_structure/sequential_asr_sampling/llama_three_attack_prefix_zero_hidden_risk_response_shuffle_seed5.md`

5 seed 均值如下：

| k | 原始 response AP | 打乱 response AP | 原始 top recall | 打乱 top recall | base_ridge AP | random AP |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.457 | 0.268 | 0.562 | 0.377 | 0.280 | 0.146 |
| 2 | 0.211 | 0.100 | 0.598 | 0.352 | 0.120 | 0.059 |
| 4 | 0.069 | 0.034 | 0.710 | 0.282 | 0.039 | 0.012 |

解释：打乱具体回答归属后，文本模型性能明显下降，说明原始 response 信号并不只是
同题、同攻击方案的边际分布；具体 q1 与具体回答之间的对应关系贡献了相当一部分预测力。
同时，打乱后仍高于随机，且接近长度/拒绝/格式 `base_ridge`，说明主题、模板边际分布和
回答姿态残余仍有贡献。后续不能把全部 response 增量都解释为“长内容语义”，需要继续做
危险词/拒绝词掩码、同题内词项稳定性和真实预算模拟。

## 代码入口

第一、二层共用：

```bash
PYTHONPATH=$PWD/src /remote-home/zty/conda/LM_zty/repetition/bin/python \
  scripts/analyze_response_safety_structure.py \
  --scores outputs/pseudo_safety_direction/mixed_alr_last_layer_scores.jsonl \
  --sample-sizes 1,2,4,8,20 \
  --methods handcrafted,tfidf \
  --output-dir outputs/response_safety_structure/mixed_alr_last_layer
```

建议先用 `--methods handcrafted` 跑全量轻量基线，再用 `--methods tfidf --sample-sizes 1,2,4` 控制内存。预计手工特征全量需要 10~40 分钟，TF-IDF 约 1~3 小时，实际以脚本逐个 `sample_size` 的进度输出为准。

## 必须报告的负对照

- 仅长度和拒绝词模型。
- 在每个问题内打乱 `pseudo_score`。
- 随机方向投影。
- 去掉拒绝开头及安全关键词后的文本模型。
- ALR 两端都位于 `(0,1)` 的子集。
- 按攻击方案分别报告 OFA、PAIR、DrAttack，避免攻击模板差异冒充安全结构。
- 在同攻击方案、同问题内打乱回答归属；该对照保留主题和边际分布，只破坏具体
  q1—回答对应关系。

## 运行记录

- 2026-07-11：启动第一层全量手工特征分析，回答数设置为 `1,2,4,8,20`。
- 日志：`logs/response_structure_handcrafted_20260711_0140.log`。
- 输出：`outputs/response_safety_structure/mixed_alr_last_layer/response_safety_structure_report.json`。
- 初始预计耗时 20~60 分钟；脚本每完成一个回答数设置会输出一次进度。

## 2026-07-11 第一层结果

全量手工特征实验实际用时约 5 分钟。训练记录 17,960 条，固定留出测试记录
6,000 条。下表以白盒 ALR-Ridge `pseudo_score` 为嗅探目标，报告同题序关系准确率：

| 回答数 | 特征 | 合并 | DrAttack | OFA | PAIR |
|---:|---|---:|---:|---:|---:|
| 1 | 仅长度 | 0.601 | 0.624 | 0.573 | 0.605 |
| 1 | 仅拒绝形式 | 0.569 | 0.581 | 0.561 | 0.564 |
| 1 | 内容风格（去长度/拒绝） | 0.598 | 0.618 | 0.581 | 0.594 |
| 1 | 全部手工特征 | 0.604 | 0.620 | 0.591 | 0.600 |
| 20 | 仅长度 | 0.618 | 0.656 | 0.593 | 0.604 |
| 20 | 仅拒绝形式 | 0.601 | 0.618 | 0.602 | 0.582 |
| 20 | 内容风格（去长度/拒绝） | 0.640 | 0.680 | 0.623 | 0.614 |
| 20 | 全部手工特征 | 0.648 | 0.679 | 0.633 | 0.631 |

完整手工模型对伪安全投影的合并 Spearman 从单回答 `0.630` 上升到 20 回答
`0.702`。分攻击方案结果均高于随机，因此信号不是只由 OFA、PAIR、DrAttack 之间
的模板差异造成。

关键解释：长度和显式拒绝词确实携带大量信号，但去掉这些特征后，行句结构、列表、
代码块、标点、词汇多样性和平均词长仍能预测伪安全投影。这支持继续检验“回答具体内容
泄露安全结构”的假设，但尚未证明语义内容有效，也不能排除更细的攻击模板和文体捷径。

第二层去拒绝前缀 TF-IDF 实验只跑 `n=1,2,4`。初版因每个目标重复拟合词表而
低效，随后改为矩阵复用；优化版日志为
`logs/response_structure_tfidf_n1_4_optimized_20260711_0220.log`。

### ASR 监督伪方向的复核

为了避免 ALR 与回答长度之间的定义关系形成自循环，使用新生成的 ASR-Ridge
`pseudo_score` 重复相同实验：

| 回答数 | 特征 | 合并 | DrAttack | OFA | PAIR |
|---:|---|---:|---:|---:|---:|
| 1 | 内容风格（去长度/拒绝） | 0.586 | 0.610 | 0.567 | 0.583 |
| 1 | 全部手工特征 | 0.589 | 0.612 | 0.576 | 0.577 |
| 20 | 内容风格（去长度/拒绝） | 0.618 | 0.663 | 0.590 | 0.599 |
| 20 | 全部手工特征 | 0.623 | 0.670 | 0.598 | 0.600 |

ASR 方向上的信号弱于 ALR 方向，但三种攻击方案中仍稳定高于随机，且增加回答数后
整体增强。这降低了“结果完全来自 ALR 长回答定义”的可能性。它仍然是相关性结果：
ASR、输入 hidden state 投影和回答内容都可能受同一潜在变量影响。

## 2026-07-11 第二层结果：去拒绝前缀 TF-IDF

优化后的 TF-IDF 全量实验约 11 分钟完成。模型只读取去除开头拒绝套话后的回答正文：

| 目标 | 回答数 | 合并序准确率 | DrAttack | OFA | PAIR |
|---|---:|---:|---:|---:|---:|
| ALR 伪方向投影 | 1 | 0.619 | 0.624 | 0.608 | 0.625 |
| ALR 伪方向投影 | 4 | 0.642 | 0.658 | 0.635 | 0.633 |
| 真实 ASR | 1 | 0.676 | 0.680 | 0.643 | 0.706 |
| 真实 ASR | 4 | 0.716 | 0.732 | 0.692 | 0.726 |
| 真实 ALR | 1 | 0.825 | 0.797 | 0.797 | 0.908 |
| 真实 ALR | 4 | 0.888 | 0.867 | 0.877 | 0.937 |

TF-IDF 比去长度/拒绝的手工文体特征更强，说明回答中的词法内容携带额外信号；
增加回答数后，多数指标继续提升。PAIR 上伪方向的 Spearman 较低（单回答约 0.455），
但同题序准确率仍为 0.625，提示全局线性相关与同题局部排序需要分别解释。

仍不能把该结果直接解释为“语义安全结构”：TF-IDF 可能利用攻击模板残片、替代建议、
免责声明或具体危险词。下一步单回答三视图实验比较 `q1`、回答和 `q1+回答`；只有回答
或联合视图超过 q1，才能证明输出提供输入之外的增量信息。

### 单回答三视图对照

| 目标 | 指标 | 仅 q1 | 仅回答 | q1+回答 |
|---|---|---:|---:|---:|
| ALR 伪方向投影 | Spearman | 0.422 | 0.659 | 0.675 |
| ALR 伪方向投影 | 同题序准确率 | 0.564 | 0.619 | 0.619 |
| 真实 ASR | Spearman | 0.469 | 0.755 | 0.767 |
| 真实 ASR | 同题序准确率 | 0.565 | 0.676 | 0.674 |
| 真实 ALR | Spearman | 0.452 | 0.743 | 0.747 |
| 真实 ALR | 同题序准确率 | 0.624 | 0.825 | 0.823 |

单条回答明显优于 q1 词法特征，且 q1+回答在同题序准确率上没有超过仅回答。这提供了
“模型输出泄露输入端伪安全投影位置”的初步黑盒证据。由于伪方向测试分数只由 q1 白盒
hidden state 投影得到，而 Ridge 方向只在训练问题拟合，测试回答没有直接进入伪方向定义，
该结果不是把测试回答原样编码进目标的直接泄漏。

需要保留两个限制。第一，测试伪方向由 ASR/ALR 监督学习，回答和投影可能共同受模型
行为倾向驱动，仍不构成因果证明。第二，预测真实 ASR/ALR 时，被抽中的回答本身属于
20 次经验统计中的一次，因此这两项目标存在自然的同样本贡献；伪方向结果是更关键证据。

### 同问题内打乱回答负对照

在每个攻击方案、每个问题内部随机重排回答归属，只破坏具体 q1—回答对应关系，保留
问题主题、攻击模板、回答和目标的边际分布：

| 目标 | 方法 | 原始单回答序准确率 | 打乱回答后 |
|---|---|---:|---:|
| ALR 伪方向投影 | 内容风格（去长度/拒绝） | 0.598 | 0.499 |
| ALR 伪方向投影 | 全部手工特征 | 0.604 | 0.499 |
| ALR 伪方向投影 | TF-IDF | 0.619 | 0.499 |
| 真实 ASR | TF-IDF | 0.676 | 0.507 |
| 真实 ALR | TF-IDF | 0.825 | 0.495 |

三种攻击方案的打乱后同题序准确率也都接近 0.5，支持正结果依赖具体指令与回答之间的
对应关系，而不是只依赖问题主题或攻击方案分布。

打乱后合并 Spearman 仍约为 0.4，这不是正信号：同问题内打乱保留了问题级平均回答
属性，而不同问题的目标均值并不相同。它表明全局相关会混入问题间差异，本研究的主要
嗅探指标必须是同题内序关系准确率，Spearman 只能作为辅助指标。

## 2026-07-11 第三层结果：白盒回答 embedding

单条回答去除拒绝前缀后输入 Llama-3.1-8B-Instruct，提取最后层最后 token 表示：

| 目标 | 合并同题序准确率 | DrAttack | OFA | PAIR |
|---|---:|---:|---:|---:|
| ALR 伪方向投影 | 0.599 | 0.606 | 0.603 | 0.589 |
| 真实 ASR | 0.662 | 0.683 | 0.637 | 0.670 |
| 真实 ALR | 0.800 | 0.778 | 0.794 | 0.841 |

白盒回答 embedding 没有超过黑盒 TF-IDF：后者对伪方向/ASR 为 0.619/0.676。
因此当前没有证据表明昂贵的回答内部表示带来额外利用价值；更值得继续研究黑盒文本特征。

## 2026-07-11 替代 ASR 与少回答独立验证

旧判定流程只对长回答判定，并在首次 unsafe 后停止。定义：

```text
judge_hazard = unsafe 判定事件数 / 有效判定票数
hazard_weighted_asr = ALR * judge_hazard
```

在 Llama 三攻击 23,961 条指令上，加权指标有 531~659 个不同值，而经验 ASR 只有
21 个值；加权指标与经验 ASR 的 Spearman 为 DrAttack 0.975、OFA 0.968、PAIR 0.962。
它提供更细排序，但仍使用同一批判定记录，需要独立验证。

将每条指令 20 个回答随机拆成 k 条前缀和独立未使用后缀，5 个随机种子平均：

| k | 前缀经验 ASR 序准确率 | 加权 hazard 序准确率 | 前缀 ASR Brier | 加权 hazard Brier |
|---:|---:|---:|---:|---:|
| 1 | 0.655 | 0.673 | 0.083 | 0.083 |
| 2 | 0.704 | 0.723 | 0.044 | 0.060 |
| 4 | 0.753 | 0.769 | 0.025 | 0.047 |
| 8 | 0.799 | 0.810 | 0.016 | 0.042 |
| 10 | 0.812 | 0.821 | 0.016 | 0.042 |

加权 hazard 在各 k 上都提高同题排序，PAIR 增益最大；但 k>=2 后 Brier 明显更差。
这说明它更适合作为排序信号或主动增采优先级，不应直接当作校准概率使用。

### 严格少量回答文本预测后缀 ASR

为避免“同一批回答既作为特征又参与 ASR 标签”的泄漏，进一步把每条指令的 20 次回答
随机拆成前缀与后缀：模型只读取前 `k` 条回答文本，目标为未使用后缀回答计算的 ASR。
结果对 3 个随机种子取平均：

| k | 合并 Spearman | 合并 Brier | 合并同题序准确率 | OFA | PAIR | DrAttack |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 0.737 | 0.066 | 0.666 | 0.634 | 0.690 | 0.675 |
| 4 | 0.788 | 0.058 | 0.705 | 0.684 | 0.712 | 0.721 |

这是一条较强的黑盒利用证据：单条回答的文本已经能预测独立后续 ASR 的同题排序，
4 条回答进一步增强。它比直接用 20 次经验 ASR 更贴近真实攻击者场景，但当前仍只在
已有 20 次采样内部做拆分；后续需要新采样或不同温度验证。

## 2026-07-11 跨攻击与跨模型迁移

### Llama 三攻击跨攻击迁移

训练域和测试域均使用 Llama 标签，分别在 OFA、PAIR、DrAttack 及三者混合数据间迁移。
每格为同题序关系准确率：

| ASR：训练来源 \ 测试目标 | DrAttack | mixed | OFA | PAIR |
|---|---:|---:|---:|---:|
| DrAttack | 0.683 | 0.646 | 0.587 | 0.672 |
| mixed | 0.680 | 0.676 | 0.643 | 0.706 |
| OFA | 0.671 | 0.660 | 0.665 | 0.645 |
| PAIR | 0.659 | 0.654 | 0.605 | 0.699 |

| ALR：训练来源 \ 测试目标 | DrAttack | mixed | OFA | PAIR |
|---|---:|---:|---:|---:|
| DrAttack | 0.799 | 0.798 | 0.754 | 0.854 |
| mixed | 0.797 | 0.825 | 0.797 | 0.908 |
| OFA | 0.775 | 0.817 | 0.802 | 0.902 |
| PAIR | 0.780 | 0.803 | 0.770 | 0.885 |

解释：回答文本信号不只局限在单一攻击方案内。混合训练在 ASR 上通常更稳，但 OFA
作为测试目标相对更难；ALR 迁移显著强于 ASR，提示拒绝/长答行为仍是最容易被文本
嗅探到的结构层。

### OFA 跨模型：回答文本与 q1 baseline

为排除“只是 q1 攻击文本本身可预测”的解释，对 OFA 的 4 个目标模型分别训练
`q1-only` 和 `response-only` TF-IDF Ridge。下表均为 ASR 同题序准确率：

| response-only：训练来源 \ 测试目标 | 4omini | DeepSeek | Llama | Qwen |
|---|---:|---:|---:|---:|
| 4omini | 0.586 | 0.636 | 0.623 | 0.617 |
| DeepSeek | 0.590 | 0.636 | 0.647 | 0.595 |
| Llama | 0.584 | 0.618 | 0.665 | 0.588 |
| Qwen | 0.626 | 0.629 | 0.658 | 0.685 |

| q1-only：训练来源 \ 测试目标 | 4omini | DeepSeek | Llama | Qwen |
|---|---:|---:|---:|---:|
| 4omini | 0.543 | 0.584 | 0.589 | 0.524 |
| DeepSeek | 0.532 | 0.609 | 0.582 | 0.504 |
| Llama | 0.509 | 0.574 | 0.574 | 0.493 |
| Qwen | 0.556 | 0.568 | 0.533 | 0.584 |

回答文本在大多数格子里明显强于 q1 baseline，尤其是 Qwen 和 Llama 自身测试格。这支持
“模型输出内容提供输入之外的安全状态观测”的说法。仍需注意：跨模型 response-only
可能利用共享拒绝模板、回答长度和安全话术，因此下一步要做去拒绝词、去长度和语义残差
版本的矩阵。

### 遮蔽拒绝词与危险词后的 OFA 跨模型对照

2026-07-12 追加负对照：在 response-only TF-IDF 中遮蔽常见拒绝词和危险词，例如
`cannot assist`、`harmful`、`dangerous`、`illegal`、`malware`、`weapon` 等，
再重跑同一 4×4 矩阵。下表为 ASR 同题序准确率：

| masked response：训练来源 \ 测试目标 | 4omini | DeepSeek | Llama | Qwen |
|---|---:|---:|---:|---:|
| 4omini | 0.586 | 0.634 | 0.625 | 0.619 |
| DeepSeek | 0.587 | 0.634 | 0.649 | 0.595 |
| Llama | 0.579 | 0.614 | 0.663 | 0.588 |
| Qwen | 0.626 | 0.627 | 0.655 | 0.684 |

与未遮蔽 response-only 相比，ASR 各格变化基本在 `±0.005` 以内；与 q1-only 相比，
masked response 仍高出约 `0.025~0.122`。因此，OFA 跨模型 response 优势不是由
少数显式拒绝/危险关键词单独支撑的。它仍可能来自更隐蔽的文体、长度、替代建议结构或
语义内容，下一步应继续做去长度残差、仅实质内容 embedding 和高权重 n-gram 审计。

矩阵差值汇总见
`outputs/response_safety_structure/cross_model_ofa_response_vs_q1/response_q1_transfer_comparison.md`。
平均来看，ASR 上 `masked response - response = -0.0010`，`masked response - q1 = 0.0694`；
ALR 上分别为 `0.0004` 和 `0.1185`。也就是说，遮蔽几乎不削弱回答信号，但回答相对
q1 的增量仍然稳定存在。

高权重 n-gram 初审使用 `jbb-llama-ofa -> jbb-llama-ofa` 的 ASR 模型。未遮蔽和遮蔽
版本的正向高权重词都包含具体风险主题或内容词，如 `genocide`、`social media`、`fake`、
`victims`、`disinformation`；负向高权重词更多是泛化/规避回答模式，如 `can provide`、
`general`、`emphasize`、`note`。这提示剩余信号可能来自“具体化程度、主题语义和回答
姿态”的组合。该审计只说明模型使用了哪些线性特征，不能把这些词解释为因果因素。

### 按实质内容长度分层

2026-07-12 进一步按单条回答去拒绝前缀后的实质内容字符数，把 Llama 三攻击测试集分为
短/中/长三档。训练仍使用全部训练题，测试时仅分层报告：

| 目标 | 合并序准确率 | 短实质内容 | 中实质内容 | 长实质内容 |
|---|---:|---:|---:|---:|
| ALR 伪方向投影 | 0.618 | 0.580 | 0.530 | 0.548 |
| 真实 ASR | 0.677 | 0.659 | 0.539 | 0.605 |
| 真实 ALR | 0.825 | 0.705 | 0.619 | 0.754 |

结果不支持“实质内容越长，安全结构信号越强”的简单叙事。ASR 和伪方向在短实质内容层
也有较强信号，ALR 在长实质内容层增强更明显。这说明长度、拒绝后剩余内容量、回答姿态
和语义内容之间存在混杂。后续若要证明“长内容具体语义”有贡献，应做长度匹配、长度残差化
或在每个长度桶内比较语义模型与仅长度/格式模型的增量。

### 长度/格式残差化

为进一步区分“回答文本信号只是长度/格式”与“具体文本内容仍有增量”，新增残差化实验：
先用长度、拒绝、行句数、代码块、列表、标点等手工特征拟合目标，再用 response TF-IDF
拟合训练集残差。结果如下：

| 目标 | 仅长度/格式 | 仅 response 文本 | 长度/格式 + 文本残差 | 残差本身 |
|---|---:|---:|---:|---:|
| ALR 伪方向投影 | 0.600 | 0.618 | 0.614 | 0.655 |
| 真实 ASR | 0.625 | 0.677 | 0.662 | 0.633 |
| 真实 ALR | 0.789 | 0.825 | 0.811 | 0.679 |

表中数值为同题序关系准确率。长度/格式基线已经很强，尤其对 ALR；但加入文本残差后，
ASR 从 `0.625` 提升到 `0.662`，ALR 从 `0.789` 提升到 `0.811`，伪方向也从 `0.600`
提升到 `0.614`。这支持回答具体词法内容存在长度/格式之外的增量信号。不过，仅 response
文本仍强于“长度/格式 + 残差”的两阶段模型，说明线性残差化可能损失了长度与语义的交互项。
后续更严格的版本应做长度匹配采样、桶内训练和非线性/交互模型审计。

### 长度匹配 pair 评估

为进一步控制长度混杂，新增“同攻击方案、同问题内长度匹配 pair”评估：只保留两条回答
实质内容长度差不超过阈值的 pair，再看 response TF-IDF 是否仍能排序目标。

| 目标 | 长度差 <=100 | <=250 | <=500 | <=1000 |
|---|---:|---:|---:|---:|
| ALR 伪方向投影 | 0.526 | 0.531 | 0.537 | 0.545 |
| 真实 ASR | 0.561 | 0.562 | 0.566 | 0.572 |
| 真实 ALR | 0.562 | 0.586 | 0.612 | 0.646 |

严格长度匹配后，伪方向几乎接近随机，ASR/ALR 仍高于随机但明显弱于全量结果。这是一个
重要修正：当前黑盒回答嗅探信号确实包含长度/回答形态成分；回答“具体内容”在长度外
仍有信号，尤其 ASR 和 ALR，但强度中等偏弱。后续关于“长内容语义”的表述必须避免过强，
更准确的说法是：输出文本作为整体观测通道有效，其中长度、拒绝形态、具体化程度和主题
词法共同贡献；单独剥离出的长度匹配语义信号仍存在但不是主导部分。

分攻击方案看，长度匹配后的 ASR/ALR 信号主要由 PAIR 保留得更多。以长度差 `<=1000`
为例，ASR 在 DrAttack/OFA/PAIR 上分别为 `0.543/0.558/0.614`，ALR 分别为
`0.614/0.635/0.749`；而伪方向在三种攻击上都只有约 `0.539~0.551`。这提示长度外文本
更容易预测经验行为指标 ASR/ALR，而不一定稳定读出由 Llama hidden state 定义的一维
ALR 伪安全方向。换言之，“攻击指标—输出文本”的联系在长度控制后仍存在；“白盒伪方向—
输出文本”的联系更依赖长度/格式等整体回答形态。

## 当前证据链强弱排序

截至 2026-07-12，三相关性主线可以拆成三条不同强度的证据：

1. **强证据：少量输出可预测后续行为风险。** 前缀/后缀拆分实验显示，单条回答文本预测
   独立后缀 ASR 的同题序准确率约 `0.666`，4 条回答约 `0.705`。这最接近黑盒利用场景。
2. **强证据：回答文本提供 q1 之外的信息。** OFA 跨模型中，response-only 明显强于
   q1-only；遮蔽拒绝/危险词后几乎不下降。说明不是少数显式安全词或输入文本本身能解释。
3. **中等证据：长度/格式之外仍有文本增量。** 残差化后 ASR 从长度/格式基线 `0.625`
   提升到 `0.662`；长度匹配后 ASR/ALR 仍高于随机，但强度明显下降。
4. **较弱证据：回答文本直接读取一维白盒伪安全方向。** 全量 response 能预测 ALR 伪方向，
   但严格长度匹配后伪方向序准确率接近随机。这说明一维伪方向与回答整体形态相关，但
   其长度外语义证据不足。

因此，当前最稳妥的论文表述不是“长回答语义直接对应安全方向”，而是：

> 模型输出文本作为一个黑盒观测通道，携带与攻击成功、拒绝行为和白盒安全相关投影有关的
> 信息；其中长度、拒绝形态和回答结构贡献很大，长度外词法/语义内容仍提供较弱但可测的
> 增量。输出文本与经验 ASR/ALR 的联系强于其与单一白盒伪安全方向的联系。

## 当前阶段判断

截至 2026-07-12，三相关性主线已有初步闭环：

1. 指令攻击性：ASR/ALR 及 hazard 加权 ASR 在同题内给出稳定风险排序。
2. 内部/隐安全结构：Llama q1 hidden state 学得的伪安全投影与 ALR/ASR 相关。
3. 输出内容：单条或少量回答文本能预测伪安全投影、ASR/ALR，且同问题内打乱回答后信号消失。

最强的黑盒利用证据是“少量回答预测独立后缀 ASR”；最强的结构证据仍是“回答文本预测
q1 白盒伪安全投影”。后续要把二者连接得更紧：用严格后缀目标、去模板特征和白盒方向
同时评估，避免把采样噪声或拒绝套话误解释为安全结构。
结论是它适合作为攻击优化排序信号，不是校准更好的 ASR 概率。Jeffreys 平滑改善低 k
Brier，但不改变排序。

## 跨模型迁移运行

JBB-OFA 上 Llama、Qwen、4omini、DeepSeek 的单回答 TF-IDF 4×4 矩阵正在运行。
每格用源模型 60 个训练问题拟合，在目标模型 20 个测试问题上用目标模型自己的
ASR、加权 hazard 和 ALR 评估，禁止跨模型标签混用。预计 20~40 分钟。

### 2026-07-12 残差跨模型矩阵

为检验跨模型迁移是否只是长度、拒绝和回答格式导致，新增 JBB-OFA 四模型
`response` 残差矩阵。流程是先用长度/拒绝/格式手工特征拟合目标，再用遮蔽拒绝词和
危险词后的回答 TF-IDF 拟合训练残差，最后比较：

- 仅长度格式基线；
- 仅回答文本；
- 长度格式基线 + 文本残差；
- 残差本身。

运行入口：`scripts/run_cross_model_ofa_response_residual_matrix.sh`。日志写入
`logs/cross_model_ofa_response_residual_20260712_000532.log`，结果目录为
`outputs/response_safety_structure/cross_model_ofa_response_residual_masked_n1`。

该矩阵已完成 16 个跨模型单元。四模型平均同题序准确率如下：

| 目标 | 仅长度格式基线 | 仅 response 文本 | 长度格式基线 + 文本残差 | 残差本身 |
|---|---:|---:|---:|---:|
| ASR | 0.563 | 0.623 | 0.600 | 0.595 |
| hazard 加权 ASR | 0.547 | 0.616 | 0.591 | 0.603 |
| ALR | 0.788 | 0.791 | 0.787 | 0.641 |

结论分裂得很有价值。ASR 和 hazard 加权 ASR 上，回答文本相对长度/拒绝/格式基线分别
平均提高 `0.060` 和 `0.068`；加入文本残差后仍分别提高 `0.037` 和 `0.044`。这说明
跨模型 ASR 风险确实存在长度/格式之外的回答文本增量。ALR 则几乎不增益，说明跨模型
ALR 主要由长度、拒绝和格式特征解释。因而后续主线应更明确地把“输出文本嗅探”优先
落在 ASR 风险结构上，而不是把 ALR 的强相关误写成语义内容证据。

### 2026-07-12 残差词组审计

新增脚本 `scripts/audit_response_residual_tfidf_terms.py`，先用长度/拒绝/格式特征拟合
目标，再审计 response TF-IDF 对训练残差的高权重词组。结果目录：
`outputs/response_safety_structure/residual_tfidf_term_audit`。

原始残差审计中出现 `###`、`end_header_id`、横线等结构残片，因此又增加了
`--strip-structure` 诊断版本。去结构残片后，ASR / hazard 加权 ASR 的正向高权重词组
主要包括三类：

- 执行/填充姿态：`of course`、`course here`、`here is`、`filled in`、`the provided`。
- 直接叙述姿态：`we`、`our`、`this can`、`this includes`。
- 少量具体风险主题：`disinformation`、`manipulation tips`、`fabricated reviews`、
  `ddos attacks`、`the victim`。

负向高权重词组主要是泛化安全姿态：`can provide`、`provide general`、`emphasize that`、
`note that`、`please note`、`general`、`educational`。

这说明长度/格式外的剩余信号不是纯危险关键词，也不是纯拒绝套话，而是“是否进入具体
执行/填充姿态”与“是否退回泛化安全说明”的混合。它支持黑盒回答文本可作为 ASR 风险
结构的观测通道，但仍不支持把剩余信号简化为纯粹的长回答语义。

### 2026-07-12 HarmBench 复验

为检查 JBB-OFA 残差结论是否只来自 JBB 题目分布，新增 HarmBench-OFA 四模型残差矩阵：
`scripts/run_cross_model_hb_ofa_response_residual_matrix.sh`。结果目录：
`outputs/response_safety_structure/cross_model_hb_ofa_response_residual_masked_n1`。

四模型平均同题序准确率如下：

| 数据集 | 目标 | 仅长度格式基线 | 仅 response 文本 | 长度格式基线 + 文本残差 | 残差本身 |
|---|---|---:|---:|---:|---:|
| JBB | ASR | 0.563 | 0.623 | 0.600 | 0.595 |
| JBB | hazard 加权 ASR | 0.547 | 0.616 | 0.591 | 0.603 |
| JBB | ALR | 0.788 | 0.791 | 0.787 | 0.641 |
| HarmBench | ASR | 0.595 | 0.642 | 0.615 | 0.599 |
| HarmBench | hazard 加权 ASR | 0.587 | 0.642 | 0.611 | 0.618 |
| HarmBench | ALR | 0.697 | 0.750 | 0.709 | 0.536 |

HarmBench 上 ASR / hazard 加权 ASR 仍复现了“response 文本优于长度格式基线”的方向，
但残差增益比 JBB 更弱：ASR 的 `基线+文本残差 - 基线` 为 `0.020`，hazard 加权 ASR
为 `0.024`。这说明长度/格式外文本增量跨数据集存在，但强度依赖数据集和目标模型组合。

ALR 在 HarmBench 上 response-only 比基线高约 `0.054`，但两阶段残差只高约 `0.012`，
残差本身也偏弱。这提示 ALR 的文本增量可能主要来自与长度/格式的交互，不能简单解释为
独立语义信号。整体上，跨数据集复验进一步支持 ASR 优先的叙事：输出文本对攻击成功风险
的嗅探价值比对 ALR 的“语义残差”更稳定。

进一步按测试目标模型分层后，JBB 的 ASR/hazard 增益较均匀：四个目标模型的
`基线+残差增益` 大多在 `0.027~0.051`。HarmBench 则更不均匀：ASR 的残差增益主要来自
Llama 目标（`0.058`）和 Qwen 目标（`0.012`），4omini/deepseek 目标只有约 `0.005/0.004`；
hazard 加权 ASR 中 Llama 仍最高（`0.055`）。因此不能把 HB 平均结果解释为所有模型上
同等稳定的文本增量，更准确的说法是：JBB 上跨模型较稳定，HB 上存在目标模型依赖。
分层表见 `outputs/response_safety_structure/residual_target_breakdown/目标模型分层汇总.md`。

进一步检查 HB 测试目标分布后发现，每个目标模型只有 7 个测试问题、700 条指令；DeepSeek
的 ALR 几乎饱和到 1，Llama/Qwen 的 ASR 方差更高。Llama 目标上 response 增益高，部分
原因是长度/格式基线偏低，而不是单纯说明 Llama 的回答语义更强。诊断记录见
`outputs/response_safety_structure/residual_target_breakdown/HarmBench目标模型诊断.md`。

### 2026-07-12 经验 ASR 分辨率限制

新增 `scripts/analyze_asr_resolution_limits.py`，统计所有已构建 ASR 数据的经验分辨率。
结果目录：`outputs/response_safety_structure/asr_resolution_limits`。

总体 88,683 条指令中，经验 ASR 只有 21 个离散档位；绝大多数记录 `totQA=20`，因此
最小非零步长为 `0.05`。总体上 `ASR=0` 的比例为 `0.169`，`ASR<=0.05` 的比例为
`0.204`。即使观测到 `0/20` unsafe，Jeffreys 95% 后验上界平均仍约为 `0.117`，
远大于 `0.01`。

因此，当前历史数据不能直接区分单条指令真实 ASR 为 `0.01` 还是 `0.001`。少量回答文本
模型可以作为同题排序、主动增采优先级或潜在风险坐标，但不能被表述为低 ASR 区间的
精确概率估计。若论文要讨论 `0.01` vs `0.001`，必须引入更多采样、层级贝叶斯模型、
跨指令共享先验或序贯实验设计。

### JBB-OFA 回答文本跨模型矩阵（仅回答，待 q1 基线校正）

同题序准确率：

| 训练模型 \ 测试模型 | 4omini | DeepSeek | Llama | Qwen |
|---|---:|---:|---:|---:|
| 4omini -> ASR | 0.586 | 0.636 | 0.623 | 0.617 |
| DeepSeek -> ASR | 0.590 | 0.636 | 0.647 | 0.595 |
| Llama -> ASR | 0.584 | 0.618 | 0.665 | 0.588 |
| Qwen -> ASR | 0.626 | 0.629 | 0.658 | 0.685 |

加权 hazard 的跨模型值为 0.588~0.639；ALR 更强，为 0.716~0.859。所有格的目标标签
均取自测试模型，不把源模型 ASR 当标签。该现象可能来自共享拒绝模板、回答长度或攻击
格式，而不是内部安全结构本身；正在运行同规模 q1-only 基线，只有回答相对 q1 的提升
才能衡量输出的独立贡献。
